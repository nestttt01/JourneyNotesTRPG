# 待辦事項

本檔只記錄尚未完成的產品工作與核准狀態。列在這裡不代表已授權修改；每次實作仍須取得專案主當次明確同意。

## 使用流程

1. 先記錄目標、範圍、不碰區域與驗收方式。
2. UI／排版工作若專案主明確要求預覽，先以瀏覽器實際渲染的 HTML/CSS 頁面供核准；未要求且需求簡單明確時，可在溝通確認後直接實作。
3. 實作與驗證完成後，從本檔移除該項，移入 `COMPLETED.md`。

## 未完成事項

### 下一階段：NPC 互動式新增／編輯

- [ ] 明日續作（最高優先）：以「認識夥伴」v15 預覽定稿 NPC 對話確認流程，並決定如何和既有逐題欄位流程銜接。
  - 目標：讓玩家感覺是在和自己建立的 NPC 對話、確認「這是不是他」，而不是替 NPC 填制式問卷；正式整合前先決定這套對話要取代、補充或只驗證既有六欄資料。
  - 流程順序：先有玩家明確寫下的人物基底，再進入「對他說一句 → 看 NPC 回應 → 玩家判斷像不像 → 必要時原句補充 → 再試一次」；情境是後段可選檢查，不是建立人物的前置條件。
  - 玩家提問：預設句要像真人交談，例如「我可以相信你嗎？」；保留玩家自行輸入，送出後聊天框必須完整回聲玩家原句，不替玩家改寫。
  - 情境對話：只有玩家明確選定情境後才使用該背景；玩家仍要輸入一句話，畫面顯示玩家原句與 NPC 回覆。每個情境各自保存未送出的草稿，切換、返回或修改不得遺失。
  - 世界資訊邊界：不得預設現代車站、親屬、地點、年代、魔法規則或其他世界觀；資料沒有寫明就維持未知，NPC 回覆不得靠固定範例自行補齊。
  - AI／金鑰邊界：目前預覽是本地固定模擬，不需金鑰、不呼叫 AI。正式版若採 AI 對話，優先評估沿用既有 `testCharacterVoice()` 與金鑰流程；不能讓簡單欄位建立本身就必須呼叫 AI，也不能在沒有角色基底時要求 AI 猜人設。
  - 寫入邊界：AI 回覆只用來讓玩家判斷方向，不自動寫成人設；玩家補充保持原句。哪些確認結果要映射到體格、語氣、喜好、厭惡、外貌、背景，仍須另外確認，未核准前不改資料。
  - 介面用語：正式名稱改用「認識夥伴」，不再使用難懂的「人物試聲」；情境入口使用「看看情境中的他」一類玩家向說法。
  - 資訊層級：一個畫面只留一個清楚任務。移除角色頭像上方的「角色配置／人物／底線」、NPC 名稱下的分類、流程狀態與 01～06 編號、「世界觀／親屬」來源籤，以及不影響當下操作的長篇寫入說明。
  - 文字層級：目前預覽以角色面板詳細頁為尺度起點，小標 10px、一般說明與選項 14px、對話 15px、輸入 16px、流程主標 20px；設計教學只影響留白與層級，不直接搬用其字級。正式套用前仍須以 computed style 複核。
  - 字型：像素字必須保留，繼續負責標題、標籤與遊戲識別；一般閱讀字體只從目前兩個候選擇一，不混搭。第二個候選較適合長文是目前建議，尚待專案主最終選定。
  - 手機排版：沿用正式專案已完成的桌機資訊架構，不回復舊手機表單。內容左緣對齊返回箭頭尖端，左右 gutter 對稱；預覽目前為 33px 內距、含邊框後左右各 35px，正式值仍須依既有 shell 實測。
  - 返回控制：正式整合改用專案的 `assets/icons/back.svg`，畫面不顯示「返回」文字，但必須保留清楚的 `aria-label`、鍵盤焦點與至少 44px 觸控範圍。
  - 三角形與動態：只用 03 像素三角形；保留輕微閒置動態，但 hover／focus 不自行切換選項，只有 pointerdown／click 選定後才移到新項目。鍵盤操作不得因動畫延遲，並保留 `prefers-reduced-motion`。
  - 預覽留存（不得刪除）：`D:\_codex_preview\journey-notes-npc-voice-flow\index.html`；本機網址 `http://127.0.0.1:4177/_codex_preview/journey-notes-npc-voice-flow/index.html?v=15`（需本機預覽服務）；SHA-256 `14b4697f3865abbd5e6e287e847c1a3f5d8948f34e31cd030387661d522a45e6`。此檔位於工作區外，保留作為明日續作範例。
  - 正式預計範圍：先唯讀定位 `app-config-ui.js`、`app-preset-edit.js`、`style.css`、`i18n.js`；只有 DOM 確實需要時才碰 `index.html`。正式實作前仍須重新確認範圍與取得明確授權。
  - 不碰區域：存檔格式、`sanko_*` key、AI prompt 與模型流程、API key、圖片傳送／匯出規則、遊戲主流程、角色面板其他分頁、首頁與全站視覺。
  - 驗收：桌機／手機實際渲染；完整走過預設提問、自訂提問、像／不像判斷、自訂調整、情境草稿與原句回聲；無水平溢位、可見原生滾輪或鍵盤延遲，zh／en／ja 與 reduced motion 均可用。
  - 預覽核准狀態：v15 已依討論完成資訊精簡與互動方向校正，僅作工作區外流程範例；正式欄位映射、AI 呼叫時機、字型定案及程式整合均尚未授權。

- [ ] 將已完成的工作區外 NPC 互動預覽，整理成正式角色配置頁可實作版本。
  - 目標：新增 NPC 時以逐題互動建立資料；保留「旅途筆記」的日記感，遊戲感由選擇節奏、三角形游標與狀態回饋提供，不改成通用後台或模板卡片。
  - 正式範圍：`#edit-scenario-screen`／`#desktop-config-editor` 的 NPC 新增與編輯流程；預計涉及 `app-config-ui.js`、`app-preset-edit.js`、`style.css`、`i18n.js`，只有 DOM 確有必要時才修改 `index.html`。
  - 互動契約：一般選項先停在待確認狀態；自訂回答走「都不像他 → 輸入 → 原句回聲 → 最終確認」；只有最終確認才寫入目前欄位，返回／修改不得遺失輸入，取消只清除未確認內容。
  - 文字安全：玩家輸入保持原文，不由 AI 改寫；完成單一欄位時不得連帶填入其他尚未回答欄位。
  - 三角形節奏：只沿用 03 的像素三角形，01／02 不得混用；hover／focus 不移動，只有 pointerdown／click 選項時切換；鍵盤操作保持即時可用，不強加位移動畫。
  - 動態前置：每次判斷互動節奏前必須重新閱讀 `emil-design-eng` skill，先回答「是否需要動、出現頻率、目的、輸入方式、easing、duration、reduced motion」。優先使用 `transform`／`opacity` 與可中斷的 CSS transition，禁止 `transition: all`，一般 UI 動態以 300ms 內為原則。
  - 尺寸統整：預覽外框只代表流程，不可直接搬入正式專案；須先依最新 `style.css` 對照角色配置 shell、雙欄／單欄斷點、內距、邊框與閱讀寬度，再統一正式版尺寸。
  - 目前對照起點：桌機斷點 1100px、外層最大寬 1580px、雙欄面板高 850px／最大高 `100dvh - 24px`；低於 1100px 維持最大寬 550px 的單欄，不把雙欄硬縮進手機。
  - 滾動原則：畫面不得出現瀏覽器原生滾輪；內容超出時仍須保留滾輪、觸控與鍵盤捲動能力，且不得造成水平溢位。
  - 不碰區域：存檔格式與 `sanko_*` key、AI prompt、API key、頭像傳輸／匯出規則、遊戲主流程與角色面板其他分頁。
  - 驗收：瀏覽器實際渲染桌機與手機；無可見原生滾輪、無水平溢位、手機操作目標至少 44px；zh／en／ja、鍵盤、Esc、`prefers-reduced-motion`、舊存檔相容與「最終確認才落盤」均通過。
  - 預覽核准狀態：流程方向已獲「效果不錯」回饋；正式外框、完整欄位題目與正式程式整合尚未核准，下一次仍須逐段確認並取得明確實作授權。

### 後續：配置表面、整站一致性與紙娃娃

- [ ] NPC 互動流程穩定後，再整理右側角色配置／情境填寫版面。
  - 目標：減少重複玻璃底與卡片層，但保留清楚的編輯區域與資料連動。
  - 順序：不與 NPC 互動流程同批修改；先盤點現況，再做工作區外瀏覽器預覽。
  - 不碰區域：不順手改首頁、遊戲頁、存檔資料或角色面板其他分頁。
  - 驗收：角色與情境欄位不遺失、桌機／手機無溢位、透明度層級一致。
  - 預覽核准狀態：尚未製作新版預覽，未授權正式實作。

- [ ] 最終盤點三種表面規則：紀錄選擇項、遊戲輸入框、角色配置輸入欄。
  - 目標：確認各自承接的閱讀層／面板層，避免重複玻璃底與不必要的全域覆蓋。
  - 視覺方向：保留日記感；潮感與遊戲感由字級、留白、選擇節奏及既有三角形建立，避免雷射線、透明度不一致、制式卡片與通用儀表板感。
  - 配套條件：若角色配置的新視覺語彙要擴及全站，先提供角色配置與首頁的成對預覽；未核准前不大改首頁。
  - 限制：不可用全域 `button`、`input`、`select` 規則直接壓過特定畫面。
  - 驗收：整理成可對照的表面／透明度／邊框規則，並以實際頁面驗證，不只看獨立樣張。
  - 預覽核准狀態：方向已確認，完整配套尚未核准。

- [ ] 紙娃娃系統先做前期規格與可行性預覽，與 NPC 互動流程分開排程。
  - 目標：確認紙娃娃是否能成為角色配置的長期視覺核心，而不是用來補救尚未統一的版面。
  - 範圍：先定義圖層類別與順序、畫布尺寸、素材格式、換裝操作、頭像輸出、儲存容量、匯出／匯入與無素材 fallback；本項不直接實作正式系統。
  - 不碰區域：未核准前不改存檔格式、不建立 migration、不把角色圖片送進 AI prompt 或任何 API，也不連動首頁改版。
  - 驗收：完成規格清單、容量與相容風險盤點，以及依專案現行風格實際渲染的工作區外預覽。
  - 預覽核准狀態：概念保留，素材方向、資料方案與正式實作均未核准。

### 角色面板收尾

- [ ] 繼續檢查角色面板其他小標題下方內容，是否遵守相同的第一字對齊規則。
  - 驗收：標題色條、標題第一字、說明與內文的起點有明確且一致的關係。

### 教學與多語言（2026-07-12 已完成大半，見 COMPLETED.md）

- [ ] 日文「人物→キャラクター」用語裁量。
  - 現況：全 ja 約 12 處含「人物」，多數（如「登場人物」）為自然日語建議保留；真正指遊戲角色需改的約 3～4 處。
  - 流程：AI 先列逐句清單與建議，專案主逐句核准後才改；不做整批機械替換。
- [ ] i18n 未引用舊鍵盤點（翻譯債）。
  - 例：API 指南留有「2. Google Gemini 與 OpenRouter」等兩家供應商時代的舊句（去重後各留一份，現行頁面已不引用）。
  - 原則：逐鍵確認 HTML／JS 均無引用才可刪，比照廢碼處理，不可只看目前 UI。

### 帳本維護

- [ ] 每隔一個大版本（如 v3.0）把 `COMPLETED.md` 的舊條目剪去 `PROJECT_HISTORY.md` 歸檔，`COMPLETED.md` 只留最近一個版本週期。
  - 目的：控制開工必讀成本，防止帳本無限膨脹。
  - 注意：`PROJECT_HISTORY.md` 未入 Git 追蹤，修改前依 `AGENTS.md` 先做工作區外交易備份；搬移時逐條剪貼、不改寫內容。
